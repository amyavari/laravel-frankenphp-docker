services:
  app:
    build:
      context: .
      target: dev
    ports:
      - "8000:80"
      - "5173:5173" # Vite development server port for Livewire hot reload
    volumes:
      - .:/app
    env_file:
      - .env
